# Environment
NODE_ENV=development
LOG_LEVEL=debug

# AWS LocalStack (for local development)
AWS_ENDPOINT=http://localhost:4566
AWS_REGION=us-east-2
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# DynamoDB Tables
DYNAMODB_TABLE=groundhog-local

# S3 Buckets
S3_BACKUP_BUCKET=groundhog-backups-local

# Secrets Manager
SECRETS_MANAGER_ENDPOINT=http://localhost:4566

# Cognito (mocked locally)
COGNITO_USER_POOL_ID=local_user_pool
COGNITO_CLIENT_ID=local_client

# New Relic (use mock for development)
NEWRELIC_API_ENDPOINT=http://localhost:3001/graphql
NEWRELIC_MOCK_ENABLED=true

# Email (MailHog)
SMTP_HOST=localhost
SMTP_PORT=1025
EMAIL_FROM=noreply@groundhog.local

# JWT (local only - not for production)
JWT_SECRET=local-development-secret-not-for-production

# Stripe (test mode)
STRIPE_SECRET_KEY=sk_test_PLACEHOLDER
STRIPE_WEBHOOK_SECRET=whsec_PLACEHOLDER
STRIPE_PRICE_ID_FREE=price_test_free
STRIPE_PRICE_ID_PRO=price_test_pro
STRIPE_PRICE_ID_ENTERPRISE=price_test_enterprise

# App
API_PORT=3000
